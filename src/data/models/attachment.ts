export interface IAttachment {
    id: string;
    fileKey: string;
    fileUrl: string;
    originalName: string;
    mimeType: string;
    fileSize: number;
    createdAt: string;
    updatedAt: string;
  }